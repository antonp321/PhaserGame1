module SpaceAdventures{
    export class Difficulty{

        index:number;
        levelSpeed:number;

        constructor(index, levelSpeed){
            this.index = index;
            this.levelSpeed = levelSpeed;
        }
    }
}